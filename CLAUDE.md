# 通用 Agent 中台项目规则

## 1. 必读基线

每个新会话首次进入本项目时，必须完整阅读：

- `docs/product-scope.md`：产品功能、用户链路和范围边界，不记录任务进度；
- `docs/project-structure.md`：目录、文件归属和依赖方向；
- `docs/backend-architecture.md`：平台 API、Agent 运行时、工作流和知识库边界；
- `docs/frontend-architecture.md`：React 工作台、状态和数据访问边界；
- `docs/development-roadmap.md`：任务依赖、状态、完成定义和验证证据。

这些文件是项目范围和架构的权威来源。实现与文档冲突时，先说明影响并在同一任务中更新基线，禁止悄悄引入第二套架构。

## 2. 第一版产品边界

本项目只建设通用 AI 中台。`automation-tool` 是独立的业务自动化产品，RPA、内容生产、社交运营等业务能力不得进入本项目 Core，也不在第一版通过能力包重新合并。

产品采用“会话优先”而不是“任务优先”。数字员工是用户正在聊天的 AI 对象；用户发送一条消息，数字员工返回一条回复。内部执行 ID、图状态和工作流步骤是实现细节，不能把普通消息塑造成需要用户理解和管理的任务。

第一版只验证一条单用户纵向链路：

```text
会话链路：选择数字员工 -> 发送消息 -> 自动检索员工绑定的知识库 -> Deep Agents 回复
工作流链路：用户手动触发，或数字员工通过公开工具触发 -> LangGraph 执行 -> 返回结果
```

第一版必须遵守：

- 不做注册、登录、用户、组织、租户、RBAC、套餐、计费和审计中心；
- 不做 Tauri、移动端、插件市场、Skill 和在线 Skill 编辑器；
- 数字员工只做最小创建、编辑、列表和知识库绑定；知识库只做创建、列表、上传文档和解析状态；
- 工作流第一版提供最小可视化编排、保存、校验和运行闭环；
- 不做任务中心，不要求用户为普通对话创建、启动或管理任务；长流程以后也优先表现为会话内进度和可继续的消息；
- 技术方案不受首版业务非目标限制，也不存在技术白名单：任何数据库、中间件、存储、运行时、协议、调度、观测和工程工具，包括但不限于 PostgreSQL、平台 Redis、消息队列、对象存储与 Worker，都可在服务当前闭环、可靠性、并发、可维护性或清晰架构时引入；采用前必须在路线图写明真实需求、正式调用路径和完成门禁，不能只为假设性未来创建空底座；
- 平台正式持久化固定使用独立 MySQL 8.4 LTS、SQLAlchemy async、`aiomysql`、PyMySQL `>=1.1.1` 和 Alembic，并通过项目专属 loopback 端口 `19506`、Compose project、容器、网络与 Volume 接入；领域和应用层只依赖仓储端口，SQLite 或其他数据库不能替代正式验收，RAGFlow 自带 MySQL 保持在其独立栈内；
- 不同时扩展多个模型供应商、多个 RAG 引擎或多个 Agent 框架；
- 优先完成真实纵向闭环，再增加管理页面、平台化抽象和企业能力；
- 尚未接通的能力必须显示真实状态，不用永远成功的 Mock 冒充已集成。

`docs/product-scope.md` 只在产品功能或范围边界真实变化时更新。任务顺序、状态、完成定义、验证证据和遗留只维护在 `docs/development-roadmap.md`；普通开发任务完成不得为了同步状态修改产品范围文档。

## 3. 路线图同步

`docs/development-roadmap.md` 是项目进度的唯一事实源：

- 开始任务前检查依赖，只允许一个任务处于 `🧪 RED` 或 `🚧 实现中`；
- 有业务逻辑的任务先写失败测试并实际运行，再写最小实现；
- 完成后立即更新任务状态、日期、RED、GREEN、真实边界、失败矩阵、清理、文档和遗留项；
- 代码、测试、配置和路线图更新属于同一个任务，禁止先宣称完成再补台账；
- 仅通过单元测试或 Demo Adapter 的集成最多标为 `🔍 待验收`；
- 外部凭据、RAGFlow 或模型服务暂不可用时，记录解除条件并继续不依赖该条件的任务；
- 依赖或完成定义变化时保留历史任务并记录原因，不删除未完成项、不降低门禁制造进度；
- 所有任务状态、当前下一步、RED/GREEN、真实边界、失败矩阵、清理和遗留只在路线图维护，禁止在产品范围、架构文档或 README 建立第二份进度台账；
- 只有任务实际改变产品功能边界或架构决策时，才在同一任务中同步相应基线文档；
- 每个任务的实现、适用门禁、真实边界、清理和路线图记录全部完成后，立即提交并推送当前分支，不等待用户逐次下令；未达完成门禁不得提前提交。

## 4. 工程结构

项目使用单一 Git 仓库：

```text
common-agent/
├── frontend/       # React + TypeScript + Vite + Ant Design
├── backend/        # Python + FastAPI + Agent/Workflow/Knowledge 适配层
├── contracts/      # OpenAPI、统一事件 Schema 和公共样例
├── infra/          # RAGFlow 等本地集成配置，不放业务代码
├── scripts/        # 跨前后端生成与验证脚本
└── docs/           # 产品、架构和唯一开发路线图
```

强制规则：

- 根目录不放前端或后端业务实现；
- 前后端独立维护依赖、测试和构建配置；
- Python/Pydantic 是 REST 与事件契约的唯一来源，前端不得手写第二套不受校验的协议模型；
- 本机运行数据、上传文件、RAGFlow 数据、日志和缓存不得进入 Git；
- 用户已明确批准阿里百炼 Demo API Key 随私有仓库版本化，作为唯一凭据例外；除该测试 Key 外，其他 Token、Cookie、密码和生产凭据仍不得进入 Git；
- 依赖版本通过清单和锁文件固定，同一工程不得并存多个包管理器锁文件。

## 5. 后端架构

- 使用 Python 3.12、FastAPI、Pydantic 和 pytest；
- 模型只直接接入阿里百炼，不建设多模型网关；API Key 从版本化的 Demo 配置读取，浏览器不得接触；
- 工作流是独立平台能力：前端用节点卡片和连线定义图，后端校验后转换为 LangGraph 执行；用户可以手动触发，数字员工也只能通过工作流的公开应用接口触发；
- 自主型数字员工通过 Deep Agents 的公开 API 接入；平台业务只依赖自研 `EmployeeRuntime` 协议；
- RAGFlow 作为独立服务，通过自研 `KnowledgeService` 和官方稳定 API 接入；
- API、运行时、工作流、知识服务和第三方适配器职责分离；API 只做协议转换和校验；
- FastAPI 单进程是最小启动形态，不是架构禁区；长任务、并发隔离、可靠重试或运行恢复需要时，可以在当前版本引入消息队列和 Worker，并纳入正式调用链验收；
- `Conversation` 与 `Message` 是用户交互的权威模型；单次回复可以有内部 `turn_id`，但不得取代会话和消息成为产品主模型；
- `WorkflowDefinition` 与 `WorkflowRun` 是独立模型，不属于数字员工定义；数字员工只能保存允许调用的工作流 ID，不能拥有或内嵌工作流实现；
- 所有运行时事件转换为会话事件，前端不得看到 Deep Agents、LangGraph 或 RAGFlow 的内部数据结构；
- Demo 模式与真实集成模式使用相同协议和状态语义，Demo Adapter 不能放宽生产错误、取消和事件顺序。

### 第三方零侵入

- 禁止修改 Deep Agents、LangGraph、LangChain、RAGFlow 或其依赖源码；
- 禁止私有 Fork、修改 `site-packages`、Monkey Patch、导入私有模块或复制内部实现后修改；
- 只通过公开 API、公开扩展点和自研适配层定制；
- 第三方类型只能存在于 `backend/src/common_agent/adapters/` 内，不能泄露到领域、API 和前端；
- 遇到上游问题依次使用公开配置、外围兼容、锁定可用版本和提交上游 Issue/PR；
- 依赖升级作为独立任务，必须运行适配器契约和纵向链路回归。

## 6. 前端架构

- 使用 React、TypeScript、Vite 和 Ant Design；聊天组件可在真实需要时使用 Ant Design X；
- Axios 负责 HTTP，TanStack Query 管理服务端状态，Zod 校验运行时响应；
- Zustand 只在出现跨页面且无法由服务端状态表达的客户端状态时引入；
- 第一版包含聊天工作台、数字员工、知识库和独立工作流设计器。聊天页显示当前数字员工和绑定知识库；工作流页负责拖拽节点、连线、配置、校验、保存、手动触发和查看结果；不建立通用任务中心、大量空菜单或占位管理页；
- 页面只依赖统一 API 客户端和平台事件，不直接调用 RAGFlow、Deep Agents、LangGraph 或模型供应商；
- 前端状态以会话和消息快照为权威，流式事件只更新当前助手回复；断流后保留用户消息并显示可重试错误，不伪造回复完成；
- 公共组件、请求封装、Schema 和常量新建前必须先搜索项目内已有实现。

## 7. 开发和测试规则

所有业务逻辑遵循 Red-Green-Refactor：

1. 先写能证明目标行为的测试；
2. 运行并确认因目标能力缺失而失败；
3. 写最小实现；
4. 运行相关测试进入 GREEN；
5. 重构后运行受影响回归。

全局采用“生产同路径验收”原则：每个对外接口和用户功能只有通过生产中原本的调用入口、正式适配层与真实依赖完成验收，才算通过。直接调用下层函数、绕过正式入口的脚本、Mock、Fake、Test Harness、进程内客户端或日志断言只能作为分层测试，不能替代该接口或功能的完成验收；若真实调用链尚未实现，相关跨端或用户功能任务最多标记 `🔍 待验收`，并把补验收依赖写入台账。测试仍须保留更低层单元、契约和故障注入覆盖，不能用端到端测试替代失败定位能力。

本项目 MVP 的生产同路径固定为：

```text
正式 React 页面
  -> 正式 Axios/SSE 客户端
  -> 真实 loopback Uvicorn/FastAPI
  -> 正式应用服务与适配层
  -> 当前任务实际采用的全部正式技术依赖
  -> RAGFlow / Deep Agents / 阿里百炼 / LangGraph
```

- 后端接口的完成证据必须通过真实监听端口访问运行中的 Uvicorn，并连接该接口在正式运行时使用的真实依赖；`TestClient`、ASGI 进程内客户端和直接调用 Service 只能做分层测试；
- 有正式页面入口的功能必须由 Playwright 在正式 React 页面完成点击、输入、上传、提交并验证最终状态；直接 HTTP、组件测试和 Test Harness 不能替代用户链路；
- 自动化浏览器测试必须使用不会创建桌面窗口的无头模式；Playwright 固定使用 `chromium-headless-shell`，不得弹出可见浏览器打断用户，只有用户明确要求人工观察界面时才允许 headed 模式；
- 每轮自动化浏览器测试无论成功、失败或中断，都必须关闭本轮 Playwright、浏览器、前端和后端临时进程，并核对没有 common-agent 浏览器残留；清理只能依据本轮记录的 PID/资源标识，不得用宽泛进程匹配误伤其他项目；
- 知识库完成必须由正式页面上传真实测试文档，经 FastAPI 正式适配层进入真实 RAGFlow，并以 RAGFlow 最终解析/检索状态为准；
- 数字员工会话完成必须由正式页面发送消息，经真实会话 API、RAGFlow 检索、Deep Agents 和阿里百炼得到回复与引用；
- 工作流完成必须由正式设计器保存节点图，经真实 API 校验、平台正式持久化、LangGraph 编译与节点执行得到最终结果；
- Demo Adapter、Mock RAGFlow、假模型和固定流式事件可以用于分层测试和不依赖外部服务的开发，但相关真实能力最多标记 `🔍 待验收`；
- 真实 RAGFlow 或百炼暂时不可用时，不停下来等待：先完成可自动化实现和失败矩阵，把真实链路任务标为 `🔍 待验收`并记录解除条件，然后继续下一个前置已满足且不依赖该条件的任务；不得把 Demo/Fake 冒充真实验收；
- 测试数据与本机演示数据必须分离；自动化测试使用隔离的平台数据库/缓存/队列命名空间、独立 RAGFlow 知识库命名和可清理文档，不污染用户保留的演示数据；
- 已建立的 Playwright 用例必须长期保留，产品行为变化时同步更新；只有对应功能正式删除后才允许按删除安全检查移除用例。

最低门禁：

- Python：pytest、Ruff、Mypy；
- TypeScript：Vitest、Testing Library、ESLint、Typecheck、Build；
- 核心用户链路：Playwright 进入数字员工会话、发送消息、观察流式回复并看到答案或明确错误；
- 协议：OpenAPI/事件 Schema 生成与漂移检查；
- 外部集成：适配器契约测试，具备凭据时再执行真实 RAGFlow、模型和 Deep Agents 验收；
- 最终门禁：后端、前端和核心 E2E 全量通过，并确认本轮服务和临时资源已清理。

分层测试职责：

| 层级 | 工具 | 负责范围 |
| --- | --- | --- |
| 后端单元 | pytest | 领域规则、状态、图校验、错误映射 |
| 后端集成/契约 | pytest + 当前正式数据库/中间件/存储/运行时/RAGFlow/百炼测试边界 | 仓储、迁移、异步任务、适配器、HTTP/SSE 契约 |
| 前端单元/组件 | Vitest + Testing Library | reducer、Schema、表单、组件交互 |
| 浏览器 E2E | Playwright | 正式 React → 正式 FastAPI → 真实依赖的用户流程 |

低层测试用于快速定位失败，E2E 用于证明真实链路；两者都必须保留，不能互相替代。

配置文件、纯文档和生成代码可不为内容本身编写单元测试，但仍需运行格式、链接、契约或生成校验。

## 8. 失败矩阵与完成定义

每个跨进程、跨层或有外部副作用的任务，开发前必须覆盖适用的失败矩阵：

- 后端未启动、超时、断流和非预期响应；
- 模型配置缺失、模型超时和上游错误；
- RAGFlow 未配置、知识库不存在、检索为空和检索失败；
- Deep Agents/LangGraph 依赖缺失、执行失败和事件映射异常；
- 重复提交、同一会话并发发送、停止生成、重试回复和晚到事件；
- 工作流缺少开始/结束、存在孤立节点、非法连线、环路、未知节点类型和节点配置错误；
- 响应 Schema 漂移、未知事件版本和敏感错误泄漏；
- 本地服务、临时文件和测试进程清理。

不适用项必须在路线图完成记录中写明原因。

此外：

- 配置、端口、上传大小、超时、状态枚举、节点类型和协议字段跨越前端、API、平台基础设施、RAGFlow 或工作流时，必须建立单一来源或契约一致性测试，禁止各层各写一份常量；
- 使用 FastAPI multipart、SSE、依赖注入、临时文件、RAGFlow 解析、Deep Agents 工具或 LangGraph 编译时，测试必须覆盖框架真实执行顺序和资源行为，不能只断言最终状态码；
- Mock、Fake 和测试适配器只能隔离未完成依赖，不能放宽正式状态、错误、序号、超时和资源生命周期；必须有契约测试证明它们对相同输入保持关键语义一致；
- 外部副作用结果无法确认时不得自动重放并声称成功；文档上传、消息提交和工作流运行需要明确幂等或结果不确定处理；
- 前后端、RAGFlow、模型和工作流的失败必须安全收敛，不允许静默回退到不检索、假回复或永远成功状态。

功能只有在以下条件全部满足后才能标记 `✅ 已完成`：

- 路线图任务中的完成定义全部满足，且实现没有偏离 `docs/product-scope.md` 的产品边界；
- 相关单元、集成、契约、类型、静态检查、构建和 Playwright 通过；
- 通过本节定义的生产同路径与真实依赖验收，并验证最终业务状态；
- 失败矩阵、安全错误、数据隔离、资源上限和恢复行为得到验证；
- 本轮临时服务、浏览器、测试数据库/知识库、容器和无用镜像完成清理；
- 同一任务内更新路线图状态、实际命令、真实边界、清理和遗留证据。

只通过 Mock、Demo Adapter、直接 HTTP 下层调用或仍缺真实 RAGFlow/百炼/浏览器门禁的任务，最多标记 `🔍 待验收`，不得降低完成定义或用文档措辞制造进度。

## 9. 安全与配置

- `.env.example` 只列变量名和安全示例；版本化 Demo 配置只允许包含用户明确批准的百炼测试 Key，禁止顺带加入其他凭据；
- 浏览器只接触自研 FastAPI，第三方凭据只保留在后端进程环境；
- 外部输入、知识片段、工作流节点配置和模型输出都视为不可信，不直接拼接 Shell、路径、SQL 或可执行表达式；
- 错误响应和日志不回显提示词全文、知识原文、密钥、内部路径或第三方响应体；
- 本地服务只绑定 loopback，开发完成后停止本轮启动的前后端和外部服务。

## 10. 本地开发与资源管理

### 本地端口与资源隔离

- 本项目启动前端、后端、RAGFlow、Docker Compose、测试服务器或其他本地服务前，必须先检查目标端口是否被占用；
- 使用项目专属 Compose project name、容器名、网络名、Volume 名、数据库/缓存/队列命名空间、对象存储 Bucket、本机路径和端口段，禁止复用其他项目的默认名称；
- 当前项目默认端口统一集中在配置文件中维护，不在脚本、测试和代码中散落硬编码；
- 发现端口冲突时只调整本项目配置，禁止停止、杀死或重配无法确认归属的其他项目进程和容器；
- 测试服务优先使用操作系统分配的随机空闲端口；需要固定端口的 E2E 使用项目专属测试端口并在结束后释放；
- 清理时只删除本项目创建的进程、容器、网络、Volume 和临时数据，不触碰其他项目资源。
- 当前日常开发机有 64GB 内存且还要运行其他任务，不能直连另一台 128GB Mac；同一个项目专属 Colima profile 在 `demo-light` 下固定为 12GiB 且不启动 RAGFlow，在完整 real 栈下固定为 32GiB，切换时必须先停止另一模式；本地 embedding/rerank 已退场并统一调用阿里百炼，32GiB 已由 R8-04 完整业务链路与 30 分钟 soak 证明长期余量，后续不得只靠静态相减或降低容器上限修改资源结论；
- 资源配置前检查本项目独立 Docker runtime/profile 当前内存、其他项目容器占用和本项目实际组件需求，为宿主机与并行项目保留余量；
- 不给每个容器无依据地拉满资源；通过启动峰值、稳定占用和检索/解析压测决定各服务限制，并把结论记录到路线图；
- 如果本项目 Docker runtime/profile 总内存不足，先按实测建议调整到合适值；不得通过裁剪 RAGFlow 必需组件或降低验证范围掩盖资源问题。
- 本项目自建镜像必须使用 `common-agent` 前缀和项目标签；任务级临时镜像还要带任务 ID 或可识别的临时标签；
- 每个任务测试完成后立即检查本项目容器引用，删除已被新版本替代、无容器引用的任务镜像、重复镜像和本项目悬空层，避免长期占用磁盘；
- 只清理能够明确归属于本项目的镜像和构建缓存，不删除其他项目镜像、仍在使用的镜像或可能被多个项目复用的官方基础镜像；
- 镜像删除前必须先列出目标、确认没有运行中或已停止但仍需保留的容器引用；清理结果写入路线图任务完成记录。

### 当前阶段运行边界

- 前端、后端、当前采用的全部正式技术依赖、RAGFlow 和浏览器测试全部在用户本机启动联调；
- 不部署远程服务器，不创建公网入口，不采购云资源；
- 阿里百炼是唯一允许访问的外部模型服务；
- 当前项目采用“稳定开发栈 + 一次性验收环境”双轨流程；
- 固定开发栈名称为 `common-agent-dev`，运行目录和数据统一位于仓库 `.local/dev/common-agent-dev/`；RAGFlow 及其不常变化的依赖可以跨任务持续复用，普通测试结束后不自动销毁；
- RAGFlow 稳定栈固定运行在独立 Colima profile / Docker context `common-agent-dev` / `colima-common-agent-dev`，不得切回承载其他项目的默认 context；
- 前端和 FastAPI 日常优先使用 pnpm/uv 本机热更新；如果后续容器化，只重建或重启本次代码变更实际影响的业务服务，禁止因单个页面、接口或测试改动重建整套 RAGFlow；
- 日常定向测试复用固定开发栈并使用唯一测试数据标识；前端、FastAPI、Playwright 和本轮临时进程在测试结束后停止；
- 里程碑最终验收使用独立数据库/缓存/队列/对象存储命名空间、随机可用端口和唯一 RAGFlow 测试知识库命名；只有变更 RAGFlow 版本、Compose、存储或跨服务基础设施时才创建完整隔离 RAGFlow 栈，结束后自动销毁；
- 稳定开发栈重建或数据卷重置后必须重放幂等 Demo Seed，并在交付前确认预置数字员工和示例工作流存在；
- 当前可复用的开发镜像每个服务只保留一个；生成新镜像后删除被替代且无容器引用的旧镜像，不为回滚长期堆积历史任务镜像；
- 一次性验收环境、测试浏览器、临时前后端进程、测试数据和任务镜像在验收结束后清理；稳定 `common-agent-dev` 仅在用户要求释放性能、暂停项目、关机前或本任务明确不再需要时停止；
- 镜像清理只针对本项目自建镜像，不碰 RAGFlow/数据库等仍在复用的官方基础镜像、其他项目镜像或任何有容器引用的镜像；
- 远程部署只有用户以后再次明确下令时才能进入路线图。

## 11. 工作方式

- 所有多步骤任务使用带 `[Tn]` 的用户可见计划，同一时间最多一个进行中；
- 先做可运行纵切，再抽象管理能力；没有第二个真实调用方时不预测性提取公共层；
- 禁止无证据的大范围重构和只为假设性未来需求提前建设空底座；当前功能或代码质量有明确需求时，技术基础设施不因首版业务范围而被禁止；
- 当前任务发现相关高风险缺陷时，先补能复现问题的失败测试，再做最小直接修复和相关回归；
- 当前任务发现范围外问题时记录到路线图，不顺手扩建；
- 每个已通过门禁的任务自动提交并推送；远程部署和云资源采购仍必须由用户另行明确授权；
- 用户已为本项目日常开发和测试明确授予持续的沙箱外执行权限。凡属于当前任务范围内的依赖下载、编译、测试、本机 loopback/端口、IPC、本地服务、Docker、无头浏览器与正式页面 E2E、Git 提交和推送，第一次就按最小范围直接发起沙箱外命令，不先在对话中重复索要口头授权，也不把沙箱失败误判为产品失败；
- 如果运行平台仍强制显示审批，使用与命令类别匹配的最小审批范围正常提交，不绕过系统拒绝。持续授权不扩展到远程部署、云资源采购、公开网络暴露、无关项目、无法恢复的破坏性删除或干扰用户正常应用；
- 如果缺少后续会持续使用的本机开发工具，遵守全局规则直接用 Homebrew 安装正式工具，先检查重复版本和冲突，不用不趁手的替代方案绕过；
- 不使用子代理，除非用户明确要求委派或并行代理工作；
- 会话中断、上下文压缩或恢复后，第一步检查 Git 工作区、运行进程/容器、任务计划和路线图“当前下一步”，确认真实状态再继续；
- 完成总结使用业务语言说明可用链路、真实限制和下一步，不把目录数量或代码行数当成果。

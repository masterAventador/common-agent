# 通用 Agent 中台 UI 对齐 demo 路线图

> 文档性质：把各功能页面布局/交互对齐 `docs/design/PowerAI Atlas.html` demo 的逐页任务台账
> 建立日期：2026-07-25
> 承接：`docs/development-roadmap-v3.md` 的 U1（UI 大工程），U1 的拆解与执行以本文件为准
> 状态说明：⬜ 未开始 · 🚧 进行中 · 🔍 待验收 · ✅ 已完成

## 1. 背景与现状

`4c796b0` 只落地了 DESIGN.md 的颜色 token、logo 和基础类，**各页面的布局与交互结构没有对齐
demo**。现状与 demo 的主要差距：

| 维度 | 现状 | demo |
| --- | --- | --- |
| 侧栏 | 宽 232px，品牌名 + 文字菜单 + 历史会话列表 | 窄 ~64px 纯图标 + hover tooltip，底部设置 |
| 页面顶部 | 无面包屑；各页 `.xxx-page` 容器带边框/圆角/阴影 | 顶部面包屑 hairline 分隔；内容区扁平、大留白 |
| 页面 header | 样式不统一 | 标题 + 一行副标题 + 右侧主按钮，底部 hairline |
| 卡片 | 样式各异 | 图标 tile + hairline + 底部"状态 + 模型"栏 |
| 对话页 | 三栏（会话列表｜消息｜员工信息），传统头像气泡，composer 嵌在消息面板 | 单栏居中对话 + 空状态引导（大标题 + 建议 chips）+ 底部悬浮大圆角 composer |

## 2. 产品边界（每个任务都必须守）

- 只复用 demo 的**布局骨架、交互结构、视觉语言**；
- **不照搬** PowerAI 名称、电力行业定位、示例账号/数据、供应商目录（PowerAI Pro/Flash 等）；
- **不引入** demo 的"团队空间""执行监控""技能库""设置"页，以及卡片上的"团队可见性/负责人"
  等团队协作字段——这些超出当前产品范围（我们是工作区 + Owner/Editor/Viewer RBAC）；
- demo 没有、我们特有的页面（工具与 MCP、审计与安全事件）：不照搬 demo（它没有），按 DESIGN.md
  的同一套设计语言与已对齐的其他页保持一致；
- 业务文案、功能、路由、数据契约继续以本项目为准，不因对齐视觉而改动。

## 3. 页面映射

| demo 页面 | 本项目对应 | 处理 |
| --- | --- | --- |
| 对话 | AI 会话 | 对齐布局 |
| 数字员工 | 数字员工 | 对齐布局 |
| 知识库 | 知识库 | 对齐布局 |
| 工作流 | 工作流 | 对齐布局（画布沿用 @xyflow） |
| 模型管理 | 模型管理 | 对齐布局 |
| 团队空间 / 执行监控 / 设置 | —（超范围） | 不做 |
| — | 工具与 MCP | demo 无，按 DESIGN.md 统一 |
| — | 审计与安全事件 | demo 无，按 DESIGN.md 统一 |
| 登录 | 登录/首位所有者 | 现状已基本对齐，仅微调 |

## 4. 任务台账

先做全局壳层（所有页的公共骨架），再逐页对齐。每个任务遵循：TDD（有交互逻辑的先写失败测试）→
相关前端门禁（lint/typecheck/vitest/build 含包体预算）→ 真实页面验收（Playwright 或正式页面
真实操作 + 截图，不用组件测试冒充）→ 更新本文件状态 → 提交推送。纯样式改动以 Playwright/真实
页面截图对照 demo + DESIGN.md 验收。

| ID | 任务 | 关键对齐点 | 状态 |
| --- | --- | --- | --- |
| UI-01 | 全局壳层：窄图标侧栏 + 面包屑 + 统一页面 header + 内容区扁平化 | 侧栏 232→64px 纯图标 + tooltip、底部入口；顶部面包屑 hairline；页面 header 统一为标题+副标题+右侧主按钮+底部 hairline；去掉各页 `.xxx-page` 的边框/圆角/阴影容器，改 demo 扁平大留白 | ⬜ 未开始 |

### UI-01 设计决策（已确认）

- **历史会话方案**：核实 demo 对话页**没有历史会话列表**（源码里的"今天"是文档更新时间/运行时间，非对话历史；demo 对话页是纯单栏）。我们的历史会话是核心功能不能丢，因此定为 **窄图标 rail（所有页）+ 历史会话迁到对话页内的历史栏**（ChatGPT/Claude 式，符合会话优先定位，视觉按 DESIGN.md）。历史迁移由 UI-01c 承接，避免中途功能回归。
- **窄 rail 内容**：logo tile → 导航图标（会话/员工/知识库/工作流/工具/模型/审计，hover tooltip）→ 底部区。工作区选择器、角色、成员、退出等本项目特有功能元素（demo 原型简化掉了）保留在顶部栏右侧。
- **建议子步（每步单独真实验收 + 提交，避免一次性大重构埋隐患）**：
  - UI-01a：Sider 232→64px 窄图标 rail + hover tooltip；顶部栏改面包屑 + 右侧功能元素保留；
  - UI-01b：内容区扁平化（去各页 `.xxx-page` 容器边框/圆角/阴影，改扁平大留白）；
  - UI-01c：历史会话从全局 Sider 迁到对话页内历史栏，保证全程可用不回归。
| UI-02 | 对话页对齐 | 单栏居中对话；空状态大标题 + 建议 chips；底部悬浮大圆角 composer（含员工/工具快捷入口）；历史会话入口按 demo 归置；气泡沿用已修的紧凑样式 | ⬜ 未开始 |
| UI-03 | 数字员工页对齐 | header + 卡片网格（图标 tile + hairline + 底部"状态 + 模型"栏）；字段仍用本项目语义（名称/描述/知识库/模型/启停），不加团队可见性/负责人 | ⬜ 未开始 |
| UI-04 | 知识库页对齐 | header + 列表/卡片 + 文档解析状态展示按 demo 骨架；上传交互保留 | ⬜ 未开始 |
| UI-05 | 工作流页对齐 | 对照 demo 工作流页；设计器三栏（节点面板｜画布｜属性/运行）视觉对齐，画布沿用 @xyflow | ⬜ 未开始 |
| UI-06 | 模型管理页对齐 | header + 模型卡片网格按 demo 骨架；沿用已去提供商行、含标识引导的卡片内容 | ⬜ 未开始 |
| UI-07 | 工具与 MCP 页统一 | demo 无此页，按 DESIGN.md 与已对齐页保持一致（header、卡片、区块 hairline） | ⬜ 未开始 |
| UI-08 | 审计与安全事件页统一 | demo 无此页，按 DESIGN.md 与已对齐页保持一致 | ⬜ 未开始 |
| UI-09 | 登录/首位所有者页微调 | 现状已基本对齐 demo，按需微调间距/文案 | ⬜ 未开始 |

## 5. 当前下一步

从 UI-01 全局壳层开始（它是所有页的公共骨架，先立骨架再逐页填充）。每完成一个并通过真实页面
验收后更新状态、提交推送。执行每一页前，先用无头浏览器打开 demo 对应页对照，再动手。
